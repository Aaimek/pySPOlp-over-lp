# Web App

The Streamlit interface lives in this folder so the numerical/backend modules can stay at the project root.

Launch locally with:

```bash
python3 -m streamlit run webapp/app.py
```

The app reuses the validated backend from the project root and is intentionally limited to a compute-then-display workflow in version 1.
